namespace GoalsApp.Api.UnitTests;

// Placeholder so the unit-test stage has something to run while there is no
// domain logic yet. Delete once the first real unit test exists (change 02).
public class PlaceholderTests
{
    [Fact]
    public void Unit_test_project_runs()
    {
        Assert.True(true);
    }
}
