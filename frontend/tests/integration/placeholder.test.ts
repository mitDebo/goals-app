import { expect, test } from 'vitest'

// Placeholder so the integration-test stage has something to run.
// Delete once the first real integration test exists (task 2.3).
test('integration test suite runs', () => {
  expect(true).toBe(true)
})
