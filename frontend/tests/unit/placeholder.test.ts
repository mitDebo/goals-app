import { expect, test } from 'vitest'

// Placeholder so the unit-test stage has something to run.
// Delete once the first real unit test exists.
test('unit test suite runs', () => {
  expect(true).toBe(true)
})
